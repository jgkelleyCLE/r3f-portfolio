import { FC, ReactNode } from "react";
interface KeycapProps extends React.HTMLAttributes<HTMLElement> {
    children: ReactNode;
    activeKey: string;
}
export declare const Keycap: FC<KeycapProps>;
export {};
